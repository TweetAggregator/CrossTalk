# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
#TODO
#GET    /                  controllers.Application.index 

# Map selection
GET     /map                        controllers.Application.index
POST	/map     					controllers.Application.submit

# Map static resources from the /public folder to the /assets URL path
GET    /assets/*file      controllers.Assets.at(path="/public", file)

# Keywords selection
GET    /enterKeywords     controllers.setup.Keywords.form
POST   /finalKeywords     controllers.setup.Keywords.finalSubmission
POST   /initialKeywords   controllers.setup.Keywords.initialSubmission

# Design example TODO: remove, should become a part of the main page
GET    /designExample     controllers.Application.designExample
